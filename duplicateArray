//  1) Write a function named duplicate that
// 	 accepts an array as its only argument and
// 	 returns a new array that contains the 
// 	 original elements followed by duplicates
// 	 of those elements.

// 	 For example:

// 		 duplicate(['🍔', '🍟', '🍺'])

// 		 would return -->
// 		 ['🍔', '🍟', '🍺', '🍔', '🍟', '🍺']

// 2) Return a new array, do not modify the 
// 	 array passed to the function.

const arr = ['🍕', '🥗', '🍷', '🍰']

function duplicate(arr) {
  let dupArr = arr.concat(arr)
  return dupArr
}

console.log(duplicate(arr))


/*------------------------------------------

Running the repl should result in the 
following output:

['🍕', '🥗', '🍷', '🍰', '🍕', '🥗', '🍷', '🍰']

------------------------------------------*/
